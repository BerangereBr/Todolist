{
    "version": 2,
    "rootDirectory": "backend",
    "builds": [
        {
            "src": "api/index.js",
            "use": "@vercel/node",
            "config": {
                "runtime": "nodejs22.x"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "api/index.js"
        }
    ]
}